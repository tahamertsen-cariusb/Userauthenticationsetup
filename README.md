
  # User Authentication Setup

  This is a code bundle for User Authentication Setup. The original project is available at https://www.figma.com/design/IBsbIFdRK16TyvWUDfR3uk/User-Authentication-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  